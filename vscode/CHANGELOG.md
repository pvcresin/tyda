# Changelog

All notable changes to the Tyda VS Code / Cursor extension are documented here.

## Unreleased

- Scaffold the extension: launch `tyda --lsp` and connect as a
  TypeProf-compatible language server for Ruby. Not yet published.
- Inline signatures (code lens) with click-to-insert `#:` comments,
  diagnostics, completion, go-to-(type-)definition, hover, and a status-bar
  toggle for the inline signatures.
